import type { Question } from "@/data/questions";
export default function QuizCard({ question, selected, onSelect, checked }: { question: Question; selected?: number; onSelect: (index: number) => void; checked: boolean }) { return <div className="glass rounded-3xl p-6"><p className="eyebrow">知识校验</p><h2 className="mt-3 text-xl font-bold">{question.prompt}</h2><div className="mt-6 grid gap-3">{question.options.map((option, index) => { const status = checked ? index === question.answer ? "border-lime-300 bg-lime-300/10" : selected === index ? "border-rose-400 bg-rose-400/10" : "border-white/10" : selected === index ? "border-cyan-300 bg-cyan-300/10" : "border-white/10"; return <button key={option} disabled={checked} onClick={() => onSelect(index)} className={`rounded-xl border p-4 text-left text-sm transition hover:border-cyan-300/50 ${status}`}><span className="mr-3 text-slate-500">{String.fromCharCode(65 + index)}</span>{option}</button>; })}</div>{checked && <p className="mt-5 rounded-xl bg-white/5 p-4 text-sm text-slate-300">💡 {question.explanation}</p>}</div>; }
