"use client";
import Link from "next/link";
import { useParams } from "next/navigation";
import { getLesson } from "@/data/lessons";
import { getLevel } from "@/data/levels";
export default function LessonPage() { const id = Number(useParams<{id:string}>().id); const level = getLevel(id); const lesson = getLesson(id); if (!level || !lesson) return <main className="p-10">关卡不存在</main>; return <main className="mx-auto max-w-5xl px-5 py-12"><Link href="/levels" className="text-sm text-cyan-300">← 返回关卡地图</Link><div className="mt-8 grid gap-6 md:grid-cols-[1fr_320px]"><article className="glass rounded-3xl p-7 md:p-10"><div className="flex items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-2xl bg-cyan-300/10 text-3xl">{level.icon}</span><div><p className="eyebrow">LEVEL {String(id).padStart(2,"0")} · 知识补给站</p><h1 className="mt-2 text-3xl font-black">{level.title}</h1></div></div><Section title="学习目标"><p>{lesson.objective}</p></Section><Section title="核心知识点"><div className="grid gap-3">{lesson.points.map((p,i)=><div key={p} className="flex gap-3 rounded-xl bg-white/5 p-4"><span className="font-black text-cyan-300">0{i+1}</span><span>{p}</span></div>)}</div></Section><Section title="交通大数据案例"><p className="leading-7 text-slate-300">{lesson.caseStudy}</p></Section></article><aside><div className="glass sticky top-24 rounded-3xl p-6"><p className="eyebrow">本关任务</p><p className="mt-3 text-sm leading-6 text-slate-300">{lesson.mission}</p><div className="my-5 rounded-xl bg-lime-300/10 p-4 text-sm text-lime-200">完成互动游戏后进入终局测验</div><Link href={`/game/${id}`} className="btn-primary w-full">进入小游戏 →</Link></div></aside></div></main> }
function Section({title,children}:{title:string;children:React.ReactNode}) { return <section className="mt-9"><h2 className="mb-4 text-xl font-bold">{title}</h2>{children}</section> }
